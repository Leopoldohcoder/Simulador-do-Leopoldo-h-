[build]
  command = "npm run build"
  publish = "dist"

# Redireciona todas as rotas para index.html (necessário para React Router em SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
